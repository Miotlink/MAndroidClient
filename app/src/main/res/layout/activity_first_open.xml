<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fl"
    android:layout_width="320dp"
    android:layout_height="420dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="center"
    android:layout_marginTop="20dp"
    android:background="#00000000"
    android:padding="0dp"
    android:scrollbars="none"
    android:scrollbarSize="0dip"
    tools:context="com.homepaas.sls.ui.FirstOpenAppActivity">



    <WebView
        android:background="#00000000"
        android:visibility="invisible"
        android:id="@+id/webView"
        android:layout_width="300dp"
        android:layout_height="400dp"
        android:layout_gravity="center"
        android:padding="0dp"
        android:scrollbars="none"
        android:scrollbarSize="0dip"
        tools:context="com.homepaas.sls.ui.FirstOpenAppActivity"/>

    <com.homepaas.sls.ui.widget.HorizontalProgressBarWithNumber
        android:layout_gravity="center"
        android:id="@+id/progress"
        app:progress_text_color="@color/appPrimary"
        app:progress_unreached_color="@color/appPrimary"
        app:progress_reached_color="@color/appPrimary"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:visibility="gone"/>
    <ImageView
        android:id="@+id/close"
        android:layout_gravity="end"
        android:background="@null"
        android:src="@mipmap/quxiao"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
    <ImageView
        android:visibility="gone"
        android:layout_marginTop="10dp"
        android:layout_marginRight="10dp"
        android:id="@+id/close_zoom"
        android:layout_gravity="end"
        android:background="@null"
        android:src="@mipmap/quxiao1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

</FrameLayout>
